const AddButton = ({ onAddClick }) => {
  return (
    <div onClick={onAddClick} className="root_addButton">
      <img src="" alt="add button" />
      <div>Add</div>
    </div>
  );
};

export default AddButton;
